import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const Color primaryBackground = Color(0xFF121212);
  static const Color secondaryBackground = Color(0xFF1E1E1E);
  static const Color tertiaryBackground = Color(0xFF2D2D44);

  // Accent colors
  static const Color primaryAccent = Color(0xFF00D4AA);
  static const Color secondaryAccent = Color(0xFF007AFF);

  // Status colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFFF6B35);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFCCCCCC);
  static const Color white = Color(0xFFFFFFFF);

  // Card colors
  static const Color cardBackground = Color(0xFF2D2D44);
  static const Color cardHover = Color(0xFF3D3D54);

  // Border colors
  static const Color borderLight = Color(0xFF3D3D54);
  static const Color borderDark = Color(0xFF1E1E1E);

  // Overlay colors
  static const Color overlay = Color(0x80000000);
  static const Color scrim = Color(0x60000000);
}
